WISHLIST
========
* - TODO
x - done!

x nosetests
    * TODO: fix the following:
     `nosetests pypsy --with-doctest` - should always work, but currently, for
     some reason, `python -c 'import pypsy; pypsy.test()'` does not run the
     doctests

* better function abstraction
* wrapper objects
     seed saving
     smart plotting
     persistence
     sweep initial paramter and get back histogram
     randomize gnerated data and get back histogram / error bars
x proper package
* CUDA capability 
     (PyMC?)
* psignifit - how it works and test comparison
* palamedes - results in test suite
* modularize graphics library
